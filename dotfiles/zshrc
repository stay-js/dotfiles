alias c="open $1 -a \"Visual Studio Code\""

alias p="cd ~/Documents/Projects"

# reinstall next.js app
alias rna="rm -rf node_modules yarn.lock .next && yarn install"

# git
alias ga="git add"
alias gb="git branch"
alias gc="git commit"
alias gpl="git pull"
alias gp="git push"
alias gs="git status"

# yarn
alias ya="yarn add"
alias yb="yarn build"
alias yd="yarn dev"
alias yi="yarn install"
alias yl="yarn lint"
alias ylp="yarn lint:fix"
alias yu="yarn upgrade-interactive --latest"